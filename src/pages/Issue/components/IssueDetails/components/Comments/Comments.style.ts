import { Box } from '@ui';
import styled from 'styled-components';

export const CommentContainer = styled(Box)`
  line-height: 1.6;
  padding: 1.5rem 0;
`;

export const CommentBody = styled.code`
  line-height: 1.6;
`;
